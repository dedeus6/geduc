server:
  port: 8080

spring:
  application:
    name: geduc
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://admin:admin@cluster0.tasilyu.mongodb.net/?retryWrites=true&w=majority&ssl=true}
      database: geduc
      prepare:
        maxConnectionIdleTime: 60000
        maxConnectionLifeTime: 0